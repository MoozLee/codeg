本次发布同步上游 `main` 的 0.14.2 能力，并继续保留 release 分支的重试编辑修复。

## 新功能与改进

- 新增 `codeg-mcp` 父进程 PID 看门狗，父进程异常退出时自动收敛子代理。
- 委派子代理超时支持设置为 0 表示不限制时长。
- 完善子代理委派工具 schema 描述文案。

## Bug Fixes

- 修复零超时配置在工具 schema 中被丢弃的问题。
- Windows 安装器在升级前先停止存活的 `codeg-mcp` 进程，避免文件占用导致升级失败。
