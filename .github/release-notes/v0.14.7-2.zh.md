本次发布修复 0.14.7-1 中会话新窗口打开与关闭后的回归。

## Bug Fixes

- 修复“在新窗口打开”会打开空窗口、未迁移目标会话的问题。
- 修复关闭新窗口后再次从会话列表打开同一会话时可能报 `a webview with label conversation-{id} already exists` 的问题。
- 修复新窗口中首次关闭由 bootstrap 打开的会话标签页时，会话可能被自动重新打开的问题。
- 在会话打开失败时显示可读错误提示，避免 Next.js 显示 `[object Object]` runtime overlay。
