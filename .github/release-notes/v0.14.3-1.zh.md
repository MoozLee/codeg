本次发布同步上游 0.14.3，并保留 release 分支上的消息编辑、供应商用量与文件工作区增强。

## Features

- 同步上游文件工作区最大化/还原能力。
- 同步上游子代理默认参数与 MCP 原生取消相关改进。
- 保留 release 分支的最后一条消息编辑、供应商用量展示与文件编辑器打开能力。

## Bug Fixes

- 保留取消后编辑最后一条消息时旧消息只在重新发送后替换的行为。
- 保留 release 分支已有迁移，避免已升级用户启动时缺失数据库迁移。
